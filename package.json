{
  "name": "browserstream",
  "version": "0.0.0",
  "description": "A place to search what is available on different streaming services",
  "main": "server.js",
  "author": "Alex Bannon",
  "scripts": {
    "setup": "npm install && bower install",
    "start": "redis-server | nodemon server/server.js",
    "test": "mocha && (webdriver-manager start &) && sleep 5 && protractor conf.js"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "cheerio": "^0.22.0",
    "db-migrate": "^0.10.0-beta.20",
    "db-migrate-pg": "^0.1.11",
    "express": "~4.13.4",
    "express-validator": "^3.1.2",
    "method-override": "^2.3.6",
    "morgan": "^1.7.0",
    "nodemon": "^1.11.0",
    "pg-pool": "^1.6.0",
    "redis": "^2.6.5",
    "request": "^2.78.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "protractor": "^5.0.0",
    "supertest": "^2.0.1",
    "webdriver-manager": "^11.1.1"
  }
}
